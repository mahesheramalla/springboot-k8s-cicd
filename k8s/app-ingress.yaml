apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springboot-app
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: springboot-app
            port:
              number: 8081